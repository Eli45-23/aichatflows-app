# AIChatFlows Admin App Environment Variables
# Copy this file to .env and update with your actual values

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# OpenAI Configuration
# CRITICAL: Get your API key from https://platform.openai.com/api-keys
# Format: sk-proj-... (starts with sk-, over 40 characters long)
EXPO_PUBLIC_OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Debug Mode (set to 'true' for development, 'false' for production)
EXPO_PUBLIC_DEBUG_MODE=true

# ===========================================
# 🚨 IMPORTANT SETUP INSTRUCTIONS
# ===========================================

# FOR OPENAI API KEY ERROR FIX:
# 1. Go to https://platform.openai.com/api-keys
# 2. Create a new API key (or copy existing one)
# 3. Replace 'sk-proj-your-openai-api-key-here' with your actual key
# 4. Make sure it starts with 'sk-' and is over 40 characters
# 5. RESTART EXPO: npx expo start -c (clear cache)
# 6. The app will now show detailed logs about API key validation

# FOR SUPABASE SETUP:
# 1. Go to https://app.supabase.com
# 2. Open your project → Settings → API
# 3. Copy the Project URL and Anon key
# 4. Replace the placeholder values above

# AFTER UPDATING THIS FILE:
# 1. Save the file as .env (remove .example)
# 2. Restart your development server: npx expo start -c
# 3. Check console logs for validation messages

# TROUBLESHOOTING:
# - If you see "Invalid OpenAI API key" → Check the key format and restart Expo
# - If AI features don't work → Verify the key at platform.openai.com
# - Environment variables not loading → Make sure file is named exactly ".env"
# - Still having issues → Enable debug mode and check detailed console logs