module.exports = {
  // Disable strict mode to reduce warnings during development
  strict: false,
  // Optional: Configure logger level
  logger: {
    level: 'warn', // Only show warnings and errors, not info messages
  },
};